<%= form_for(current_user.wishlists.build) do |f| %>
  <div><%= hidden_field_tag :property_id, @property.id %></div>
  <%= f.submit "Add to wishlist",
               class: "btn btn-block btn-outline-success" %>
<% end %>