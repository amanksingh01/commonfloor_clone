Hi <%= @seller.name %>,

<%= @user.name %> is interested in your property, "<%= property_title(@property) %>", <%= property_url(@property) %>.

The contact details of <%= @user.name %> are as follows:

Email: <%= @user.email %>
Mobile Number: +91 <%= @user.mobile_number %>

You can also view the list of all the users interested in this property by navigating to the link below:

<%= interested_users_property_url(@property) %>